apiVersion: gh-infra/v1
kind: FileSet
metadata:
  owner: to4iki

spec:
  repositories:
    - Store
    - funpou

  files:
    - path: .github/CODEOWNERS
      content: |
        * @to4iki
    - path: LICENSE
      source: ../templates/LICENSE

  via: push
