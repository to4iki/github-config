apiVersion: gh-infra/v1
kind: Repository
metadata:
  name: herdr-fzf-terminal-browser
  owner: to4iki

spec:
  description: "A herdr plugin: press a key, pick a URL printed in the current pane with fzf, and it opens in terminal-browser"
  visibility: public
  topics:
    - herdr
    - herdr-plugin
    - terminal-browser
    - rust
    - fzf

  features:
    issues: true
    projects: false
    wiki: false
    discussions: false

  merge_strategy:
    allow_merge_commit: true
    allow_squash_merge: true
    allow_rebase_merge: false
    auto_delete_head_branches: true

  rulesets:
  - name: protect-main
    target: branch
    enforcement: active
    conditions:
      ref_name:
        include:
          - refs/heads/main
    rules:
      pull_request:
        required_approving_review_count: 0
        dismiss_stale_reviews_on_push: false
        require_code_owner_review: false
        require_last_push_approval: false
        required_review_thread_resolution: false
      non_fast_forward: true
      deletion: true
      creation: false
      required_linear_history: false
      required_signatures: false
